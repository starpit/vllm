# Vision DSL — Phase G handoff

**Branch:** `feat/rust`, worktree `.claude/worktrees/multimodal`.
**Goal:** lift the vision tower into a `#[vision_forward]` DSL body, mirroring how text decoders are described. Each MM arch ends up with two backbones — text (`#[forward]`) and vision (`#[vision_forward]`) — composed at runtime via the existing `MmEmbedSplice` op. Supersedes the MULTIMODAL_HANDOFF.md "Phase G — Gemma3-MM / SigLIP" framing, which assumed a non-DSL `ferrite-vision` shared module.

**Why now:** with 3+ VL/MM arches imminent (Qwen2-VL + Qwen2.5-VL + Gemma3-MM/SigLIP, internvl/llava/pixtral on the horizon), the imperative ~1k-line per-arch `vision.rs` (931 + 989 lines today) is the wrong cut. Hybrid DSL gives per-block fusion, const-prop, and reduces each new tower to ~30 lines of math. The first port is the expensive one; each subsequent arch is the payoff.

## Phases

**G.1 — `ferrite-vision` host-glue crate. (DONE.)** Lifted from both VL crates: `VisionConfig` (common geometric fields) with `build_rope_cos_sin_bf16` + `patches_from_normalized_chw` as methods, plus free-fn `build_cu_seqlens_i32`, `pad_linear_k_to_mult8` (Q2.5-VL cuBLAS K=3420 fix), `TraceDump`, byte-slice helpers. Both VL crates re-export `pub use ferrite_vision::VisionConfig` and now carry only their arch-specific extras (Qwen2.5-VL: `VisionExtras { intermediate_size, window_size }`). Behavior-preserving — no DSL changes.

**G.2 — New OpKinds in `ferrite-forward-macro`. (DONE.)** Landed `7f0dabec3`: four new variants in the `OpKind` enum (`classified.rs:67`) — `VarlenAttention`, `VisionRope`, `QuickGelu`, `GeluErf`. Existing `Gelu` stays tanh-form. Each variant has a shape signature in `shape.rs` (`sig_varlen_attention`, `sig_vision_rope`, plus arms on `sig_unary_elementwise` for the two GELUs) and a corresponding `weight_arg_ranks` row. `Stmt::AssignTuple` grew a third arm for `(q, k) = vision_rope(...)`. Six unit tests in `shape::tests` lock the sigs. **Deliberately deferred:** `from_name` arms — they land in lockstep with G.4 Impls + G.5 DSL bodies so parse ⟺ codegen stays total (no parse-then-reject). Until then the new OpKinds are produced only via direct `apply_signature` calls (i.e., from tests).

**G.3 — `#[vision_forward]` attr macro. (DONE.)** Both attribute entries call a shared `compile_common(args, carrier, mode: CompileMode)`; the mode gates three local overrides — DSL prelude (`Prelude::Decoder` vs `Prelude::Vision` threaded through `classify::classify_with`), tp fanout (decoder fans `{1, 2, 4, 8}` at nccl-enabled, vision pinned to `[1]`), and the post-FUF lowering passes (`insert_all_reduces` / `insert_lm_head_allgather` / `insert_mm_splices` skipped for vision). `ExternKind` grew six vision variants (`Pixels`/`CuSeqlens`/`Cos`/`Sin`/`GridThw`/`MaxSeqlen`) with a `from_name_for(name, prelude)` that keeps the two extern sets disjoint — `pixels` is unrecognized in a decoder body and `input_ids` in a vision body. `extern_shape` got arms for the new variants (`Pixels`/`Cos`/`Sin` shapes anchor on `vision_in_features` / `vision_rope_half_dim` bounds populated per-arch in G.5; the rest are opaque). 4 new classify unit tests pin the routing; llama-2-7b text decoder still emits 484 tiles · 228 waves byte-equivalently. Workload axis reuse: vision keeps the existing `workloads = [...]` slot and reads it as **batched-total flat L** (not per-image) — same axis as text-side `num_tokens`.

**G.4 — Implementations for the new OpKinds. (DONE.)** Four singleton Impls in `impl_lib.rs` — `VarlenAttentionImpl` / `VisionRopeImpl` / `QuickGeluImpl` / `GeluErfImpl` — each claiming its OpKind via `single_tile_match` and emitting one new `Instruction<W>` variant. End-to-end wired: `from_name` arms (`varlen_attention` / `vision_rope` / `quick_gelu` / `gelu_erf`) → Impl → `Instruction` variant → eval body calling the existing kernel. New surface: four `CanonicalParams::VISION_*` consts (default 0; set by `#[vision_forward]` from the vision config in G.5) and two `ForwardCtx` fields (`vision_rope_cos`/`vision_rope_sin` Option-views, `None` for text-side calls). Llama-2-7b text decoder still emits 484 tiles · 228 waves byte-equivalently. Cost CSV calibration deferred to G.5+ (analytic `cost_attention` / `elementwise_cost` placeholders).

**G.5 — Port Qwen2-VL.** Rewrite `ferrite-model-qwen2-vl/src/vision.rs` so the encoder is a `#[vision_forward] fn qwen2_vl()` body — patch_embed (`gemm` + synthesized `reshape`), 32-block loop, ln_q + merger reshape + merger MLP all inside the DSL. Imperative `VisionWeights::forward` deletes ~200 lines (block loop + merger MLP path); the orchestrator wrapper retains pixels packing + `cu_seqlens` / cos-sin building (now via `ferrite-vision`) + a single call into the generated `qwen2_vl_vision_forward(...)`. **Verification:** the three bug reproducers (`test_qwen2_vl_bug{1,2,3}_*`) plus the byte-equivalence smoke (224² red circle + streamlit screenshot) all pass.

**G.5 sub-phases (post-survey).** A first pass surveying current state surfaced five blockers behind the original G.5 framing — addressed in order so each layer is independently verifiable.

- **G.5.a — `CanonicalParams::VISION_*` codegen bake. (DONE.)** Extended `emit_canonical_params_impl` in `codegen.rs` so every emitted `Weights` impl carries `VISION_NUM_HEADS` / `VISION_HEAD_DIM` / `VISION_Q_SIZE` / `VISION_ATTN_SCALE` consts driven by `model.bounds.get("vision_num_heads" / "vision_head_dim")`. Text-only configs lack those keys → emitted values are 0 / 0.0, which match the trait defaults declared in `ferrite-forward::CanonicalParams` — text path is byte-equivalent. Validated: `cargo build -p ferrite-forward-macro` green; `FERRITE_MODELS=command-r-1-layer cargo build -p ferrite-model-commandr --features cuda` green.

- **G.5.a.1 — Decoder-specific guard rails to gate. (NOT YET DONE — discovered during G.5.b prep.)** `compile_common` calls `collect_dispatch_bounds` (`lib.rs:1094`) which **panics** if a model config lacks any of `DISPATCH_FIELDS = {num_hidden_layers, hidden_size, intermediate_size, num_attention_heads, num_key_value_heads, head_dim, vocab_size}` — every one of those is decoder-specific. Vision configs don't carry them. The G.3 `CompileMode::VISION` split only gates the prelude / tp fanout / mm-splice steps; the dispatch-bounds + `apply_hf_defaults` (head_dim ← hidden_size / num_attention_heads) + arch-dispatcher emission (`FerriteArchRegistration` vs the existing hand-written `FerriteMmRegistration`) all still assume decoder. Owed before G.5.b can land:
  1. Either skip `collect_dispatch_bounds` under `CompileMode::VISION` (and accept a separate vision dispatcher that doesn't expose `num_hidden_layers` etc.) OR populate vision-side stubs (e.g. `num_hidden_layers ← vision_depth`) with a clear "this is a vision config" tag so `vllm ferrite info` doesn't conflate them.
  2. `emit_arch_dispatcher` must NOT emit `FerriteArchRegistration` for vision (text-side `qwen2` already covers `Qwen2VLForConditionalGeneration` via its hf_arches union). The vision body's emitted `Weights` should be reachable via the existing hand-written `FerriteMmRegistration` in `vision.rs:593` — i.e. the macro emits the struct + forward fn + impls, but no inventory submission, and the hand-written `try_load_mm_qwen2_vl` constructs and returns a `Box<dyn MultimodalForward>` over a `VisionWeights` that wraps the macro-emitted `Weights`.
  3. `apply_hf_defaults` (`config.rs:946`) must not fire its `head_dim` derivation for vision configs (no `hidden_size` to read).
- **G.5.b — `ferrite-model-qwen2-vl/configs/` plus weights.json.** Currently the crate has only `Cargo.toml` + `src/`. `discover_models_dir` will fail the first time `#[vision_forward]` runs there. Owed: a per-variant config (`qwen2-vl-2b.json` / `qwen2-vl-7b.json` / `qwen2-vl-72b.json` — one per `d_model`; vision tower itself is shape-identical across the family) carrying the `vision_*` bounds (`vision_embed_dim=1280`, `vision_depth=32`, `vision_num_heads=16`, `vision_head_dim=80`, `vision_mlp_hidden=5120`, `vision_spatial_merge_size=2`, `vision_in_chans=3`, `vision_temporal_patch_size=2`, `vision_patch_size=14`, `vision_in_features=1176`, `vision_rope_half_dim=40`, `vision_merge_factor=4`, `vision_merge_hidden=5120`) plus `d_model` per variant, plus `architectures: ["Qwen2VLForConditionalGeneration"]` and `tie_word_embeddings: false`. `weights.json` declares the visual.* tensor shapes anchored on those bounds.
- **G.5.c — Reshape DSL surface (handoff §"Open question 2" — deferred from G.3).** Merger reshape `[L, vision_embed_dim] → [L / vision_merge_factor, vision_merge_hidden]` cannot be expressed today (`OpKind::Reshape` is synthesis-only; `from_name` rejects `reshape(...)`). Owed: minimal addition — accept `out = reshape(x, [a, b])` in `parse.rs::Stmt::Assign` with literal-or-bound second arg, route through `OpKind::Reshape`, and record the target shape on `Program::reshape_targets` like `apply_reshape_hints` does. Test on a unit `codegen::tests` body before authoring the real qwen2_vl body.
- **G.5.d — `extern_shape` arms for vision.** `shape::extern_shape` in `shape.rs` currently anchors `Pixels` / `Cos` / `Sin` on `vision_in_features` / `vision_rope_half_dim` bounds — confirm those keys flow through G.5.b's config and that opaque externs (`CuSeqlens` / `GridThw` / `MaxSeqlen`) stay rank-1 placeholders the FUF treats as scalars. No code change expected if G.5.b lands the bound keys correctly; included as an explicit checkpoint.
- **G.5.e — Stub `#[vision_forward] fn qwen2_vl()` body.** Minimal pass-through (e.g. `out = layer_norm(pixels, merger.ln_q);`) authored in a NEW `src/dsl_body.rs`, NOT consumed by the host wrapper yet. Validates the wiring chain end-to-end (configs → bounds → CanonicalParams → manifest → classify → FUF → solver → codegen → emitted `qwen2_vl_vision::forward(...)`) without disturbing the imperative `VisionWeights::forward` path. Build green = G.5.a–G.5.e foundation done.
- **G.5.f — Real body port.** Replace the stub with the full encoder math: patch_embed gemm, 32-block loop (norm1 + qkv gemm + bias_add + vision_rope + varlen_attention + proj + add + norm2 + fc1 + bias_add + quick_gelu + fc2 + bias_add + add) → ln_q + reshape + mlp_0 + bias_add + gelu_erf + mlp_2 + bias_add. **Same e2e verification as the original G.5 framing.** The qkv gemm + bias is the established Qwen2-pattern (text-side `qwen2()` already shows it); reshape arrives via G.5.c.
- **G.5.g — Host wrapper rewrite.** `VisionWeights::vision_forward` builds `cu_seqlens` / cos / sin / max_seqlen GPU tensors (already does), constructs a `ForwardCtx` with `cu_seqlens_q` / `max_seqlen_q` / `vision_rope_cos` / `vision_rope_sin` populated, calls `qwen2_vl_vision::forward(weights, &ctx, device, total_l)`. Drop the imperative block loop + merger MLP code (~200 lines). Verification: `test_qwen2_vl_bug{1,2,3}_*` green + streamlit byte-equivalence retained.

**Where to start next session:** G.5.a.1 — extend `CompileMode::VISION` to gate `collect_dispatch_bounds` + `emit_arch_dispatcher` (no `FerriteArchRegistration` for vision) + `apply_hf_defaults` head_dim derivation. Until that lands, any vision config will hit the `collect_dispatch_bounds` panic at macro expansion. Only after G.5.a.1 can G.5.b's configs be parsed without crashing the build.

**G.6 — Port Qwen2.5-VL.** Same shape as G.5. Two block flavors (full vs windowed varlen) — pick after seeing G.5's emitted code whether that's two `#[vision_forward]` bodies, one body with a runtime-conditioned varlen-attn op, or a `fullatt_block_indexes`-driven `if` inside the body. K=3420 cuBLAS pad trick lives in `ferrite-vision` (load-time weight pad + runtime concat-with-pad before `silu_and_mul_fused`). Same e2e tests; same byte-equivalence smoke.

**G.7 — SigLIP / Gemma3-MM.** Third arch lands as a ~30-line `#[vision_forward]` body in a new `ferrite-model-gemma3-mm` crate. **If it's longer than ~50 lines, the DSL is missing an op — fix the DSL, not the arch.** This phase is the test that we got the cut right.

## Non-goals

- No TP for vision in v1. Replicated weights stay; no AllReduce/AllGather extension.
- No new pyo3 / vllm-cuda / cuda_worker surface. Touch budget on those crates: 0 lines.
- Existing imperative `vision.rs` files don't survive G.5/G.6 — they're rewritten, not paralleled. (Per the "no parallel impls" feedback.)
- `ferrite-vision` is host-glue ONLY. GPU compute lives in OpKinds. If a helper does `kernels::*` work it belongs as an OpKind, not a shared function.
- No attempt to merge text and vision into one body. They run at different times in the request lifecycle, with different KV semantics — keep them as peer compiled forwards.

## Open questions to resolve before G.3

1. **Workload bucketing key.** RESOLVED: bucket on **batched-total flat L** (the concatenated varlen length the kernel actually launches over), not per-image. Mirrors text-side `num_tokens` semantics — total across the batch, not per-sequence. Default sweep `workloads = [256, 1024, 4096, 16384]` covers single-image 224² (~256 patches), 448² (~1024), 672² (~2304), and ~4-image streamlit-class (~16384). The vision macro reuses the existing `workloads` attribute slot — no new key. Per-image `max_seqlen` is a kernel input, not a solver axis.
2. **Reshape DSL surface.** RESOLVED FOR G.3: deferred. `OpKind::Reshape` stays synthesized-only through G.3. The vision body has two explicit reshapes (`[L, H, D] → [L, H*D]` post-attention, `[L, embed] → [L/S², embed*S²]` pre-merger-MLP); the first is recoverable by per-head shape inference (same shape as text's QK-norm path), the second requires bound-arithmetic in the reshape target dims (`l_out = num_tokens / merge_factor`, `merge_hidden = embed_dim * merge_factor`). G.3's stub vision body avoids the merger reshape; the DSL surface for it lands in G.5 when we actually port qwen2-vl.
3. **Tuple-returning ops in the DSL.** RESOLVED: 2-target works. `vision_rope_arity_and_output_shape` test path validates `Stmt::AssignTuple` for 2-target via the `vision_rope` shape signature; 3-target is exercised by `MlaSplit`. No further code change needed.

## Where to start

G.1 + G.2 + G.3 + G.4 + G.5.a done. Next: **G.5.a.1** — gate decoder-specific compile steps (`collect_dispatch_bounds`, `emit_arch_dispatcher`, `apply_hf_defaults`) under `CompileMode`. See G.5 sub-phases section for the full ordering through G.5.g.

**G.5 entry notes** (for the next session):
- Vision-config bounds need population. The four new `CanonicalParams::VISION_*` consts (`VISION_NUM_HEADS`, `VISION_HEAD_DIM`, `VISION_Q_SIZE`, `VISION_ATTN_SCALE`) currently default to 0 / 0.0. The `#[vision_forward]` macro path lifts them from the per-arch `VisionConfig` (already in `ferrite-vision`) and emits the `impl CanonicalParams for Weights` arms. Same as the existing text-side bake.
- `ForwardCtx::vision_rope_cos` / `vision_rope_sin` need population. The `vision_forward` host wrapper (where pixels packing + cu_seqlens / cos / sin builders live today) constructs the cos/sin GPU tensors per-call from `grid_thw`, then writes a fresh `ForwardCtx` with these fields `Some(view)` before invoking the generated vision interpreter. Text-side `ForwardCtx` constructions (in `vllm-executor::cuda_worker`) already pass `None` — the `Instruction::VisionRope` arm panics on `expect` if reached without these set, mirroring `Instruction::AllReduce`'s `tp_group` contract.
- `extern_shape` for the vision externs (Cos/Sin/Pixels) anchors on bounds `vision_in_features = c*t*p*p` and `vision_rope_half_dim = head_dim/2`; these must be populated in the vision config before the FUF's shape-inference closes.
- Imperative `vision.rs` deletes ~200 lines (block loop + merger MLP path); the orchestrator wrapper retains pixels packing + cu_seqlens / cos-sin building + a single call into the generated `qwen2_vl_vision_forward(...)`. Verification: the three bug reproducers (`test_qwen2_vl_bug{1,2,3}_*`) plus the byte-equivalence smoke (224² red circle + streamlit screenshot) all pass.

## Verification at every phase

Per the "integration test per phase" feedback rule: each phase ships with the existing e2e suite (`test_qwen2_vl_bug{1,2,3}_*` + tp=2 sanity) green. No phase commits with red e2e tests. The byte-equivalence smoke against the streamlit-screenshot prompt is the canary — if it stops naming "vLLM Chat Assistant" we lost the math somewhere.
