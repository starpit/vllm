node_modules
dist
# Top-level repo .gitignore blocks bare `lib/` (Python/Rust convention).
# Allow it back inside the SPA's src tree.
!src/lib/
!src/lib/**
.DS_Store
# tsc emit (we use vite for the actual build; tsc -b is type-check only)
*.tsbuildinfo
vite.config.d.ts
vite.config.js
# Generated by `FERRITE_VIZ_OUT=vllm-rs/viz/public/arches cargo build -p ferrite-models`.
# Regenerate locally; don't commit (llama.json is ~19 MB).
public/arches/
